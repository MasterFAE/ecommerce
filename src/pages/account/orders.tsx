import React from "react";
import AccountLayout from "../../components/AccountLayout";

type Props = {};

const Orders = (props: Props) => {
  return <AccountLayout>Orders</AccountLayout>;
};

export default Orders;
