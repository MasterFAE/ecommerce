import React from "react";
import AccountLayout from "../../components/AccountLayout";

type Props = {};

const Tickets = (props: Props) => {
  return <AccountLayout>Tickets</AccountLayout>;
};

export default Tickets;
